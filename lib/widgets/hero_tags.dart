/// Shared Hero tags used to visually connect widgets across screens.
library;

/// Shared between the splash screen's coin and the profile coin icon in
/// Home's top bar, so the splash coin flies into place instead of cutting
/// away when the app finishes launching.
const heroAppCoinTag = 'appCoin';
